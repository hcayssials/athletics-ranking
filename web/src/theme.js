// Ranking What-If Studio palette — "WA Editorial" (light), inspired by worldathletics.org:
// white surfaces, cool near-black ink, one bold WA-red accent. Colours live here so the
// look can be retuned in one place. (Some incidental shades are still inline in App.jsx
// inside composite strings like "1px solid #d8dce2"; they use the same values as below.)
export const BG = "#ffffff";       // page background
export const SURFACE = "#f2f4f7";  // cards / panels / bars (was CREAM)
export const INK = "#14181f";      // primary text (cool near-black)
export const MUTE = "#6b7480";     // labels / secondary text
export const ACCENT = "#e8112d";   // WA red — arrows, headline numbers, CTAs (was GOLD)
export const MONO = "'IBM Plex Mono', monospace";

// Reference roles for the incidental shades used inline in App.jsx (kept here as the
// single source of truth for the palette):
//   surface-alt  #e7eaef   border  #d8dce2   faint text  #9aa1ac   body text  #3a414c
//   positive     #1f8a4c (bg #e3f3e9 / fg #1f6b43)   negative #d12b2b (bg #fbe4e1 / fg #9c352a)
//   neutral chip #eceef2 (fg #5b6573)                info     #2f4a6b (bg #eef2f7)
